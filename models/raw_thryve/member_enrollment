select
        member_id,
        plan_id,
        enroll_date,
        lower(status) as status,
        cancel_date,
        initcap(region) as region
    from raw_thryve.raw_thryve.MEMBER_ENROLLMENTS
    where status ='cancelled'